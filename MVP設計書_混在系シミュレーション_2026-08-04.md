# MVP設計書 — 異認知圏エージェント混在シミュレーション

作成: 2026-08-04 ／ 対象: AUTOMATA Vol.2 GPU申請（締切 8/9）添付MVP
実装担当: Claude Code（Sonnet 系＝maker、Opus 系＝checker）

---

## 0. この文書の使い方

- §1〜§8 が設計本体（人間＝Su の承認対象）。
- §9 が maker 用ブリーフ、§10 が checker 用ブリーフ。**そのまま Claude Code に貼って使う**。
- 未確定事項は §11。実装開始前に Su が決める。

---

## 1. MVPの目的（操作的定義）

GPU申請フォームのMVP欄に添付できる public リポジトリを 8/8 までに作る。「動く」の定義:

1. **エージェント別モデルルーティング**が動作する（ブロックごとに異なるOllamaモデル／エンドポイントを割当て）
2. ローカルPC（12GB VRAM）で **3系統混在のsmoke run が完走**し、jsonl一次データが出る
3. **語彙伝播の測定スクリプト**が jsonl から「ブロック固有語彙の越境」を定量出力する

この3点で「小型では動く。大型系統の同時常駐にはVRAMが物理的に不足」という申請文が実測付きで書ける。

## 2. スコープ

### やる（MVP）

- 新規最小エンジン（下記§3の理由でフォーク拡張ではなくスクラッチ実装）
- 2D格子世界＋places（数値情報のみ）、通信半径＋同一領域制約、memory自己フィードバック、4フェーズ同期ループ（Vol.1と同一の実行順序＝比較可能性の担保）
- ブロック→モデル/エンドポイント割当のconfigスキーマ
- jsonl ログ（Vol.1互換フィールド＋ `bloc` / `model` 追加）
- `tools/vocab_metrics.py`（ブロック固有語彙の抽出と越境タイムライン）
- smoke config（3系統×小型モデル）と README

### やらない（本選期間に回す）

- 火事等の環境イベント（フックだけ用意しない。本選で追加）
- 動画生成・ビューア（Vol.1リポジトリのツールを後で流用可能な log 互換性のみ確保）
- 日本語プロンプト切替（MVPは en のみ。小型モデルのJSON安定性を優先）
- ペルソナ（性別等）— **ブロック間の差はモデル重みのみ**。介入最小主義の核
- GPUクラスタでの複数エンドポイント実測（configは対応済み、検証は8/15以降）

## 3. リポジトリ戦略（重要な設計判断）

**新規リポジトリ・スクラッチ実装**とする。理由:

- Vol.1で「ベース基盤への依存が強い」評価を受けた。authorship を構造的に明確化する
- ルーティングは agent→LLM 呼び出しの根幹に触るため、どのみち改修範囲が大きい
- エンジン全体で目安 500〜800 行。実装エージェント使用で1〜2日で書ける規模

**クリーン実装の規律**: 実装エージェントは **ryukih 氏の元コード・Su の既存フォークのコードを一切読まない**。本設計書の挙動仕様のみから実装する（§9 ブリーフに明記）。挙動互換（フェーズ順序・通信制約・ログフィールド名）は本書がスペックとして保証する。

- README に元パラダイム（ryukih 氏の火事シミュレーション観察課題）へのクレジットを明記
- ライセンスは §11 の未確定事項（コード非流用ならGPL継承義務はないが、Su の判断待ち）

## 4. アーキテクチャ

```
mixed-cognition-sim/          # リポジトリ名は仮（§11）
├── engine/
│   ├── world.py              # 格子空間・places・距離計算
│   ├── agent.py              # 状態（位置・bloc・memory・message履歴）
│   ├── llm_client.py         # Ollama API（モデル名＋base_url可変、リトライ、JSON抽出）
│   ├── prompts.py            # プロンプトテンプレート（英語、数値のみ）※凍結対象
│   ├── sim.py                # 4フェーズループ・ロギング
│   └── config.py             # YAML読込・バリデーション
├── main.py                   # CLIエントリ（--config）
├── configs/
│   ├── smoke_local.yaml      # 3系統×2体×15step（PC1想定）
│   └── mvp_demo.yaml         # 3系統×4体×30step（申請添付run）
├── tools/
│   └── vocab_metrics.py      # 語彙伝播測定
├── output_*/                 # run毎（jsonlはcommit、他は除外 = Vol.1の3層区別を踏襲）
├── README.md
└── requirements.txt          # requests, pyyaml のみ（重い依存を増やさない）
```

## 5. 挙動仕様

### 5.1 世界

- 原点中心の格子。`half_space_size` で範囲 [-S, +S]。座標は整数
- places: `{name, center_x, center_y, half_size, capacity}` のリスト。境界含む矩形
- 全エージェントは全placesの**位置**を知る。**占有状況（人数・capacity・占有率）はその場所内にいるエージェントのみ**プロンプトで受け取る
- 初期位置は seed 付き乱数で場所外に一様配置

### 5.2 ブロックとエージェント

- config の `blocs` 配列: `{name, model, base_url, num_agents, llm_overrides{}}`
- agent_id は全体通し番号。**プロンプトには bloc・モデル名を一切含めない**（エージェントは自他のモデルを知らない。blocは観察者側ラベル）
- エージェント状態: position, memory リスト（上限 `memory_limit`、推論参照 `memory_size`）、受信メッセージ履歴（上限 `message_history_limit`、推論参照 `message_context_size`）

### 5.3 通信

- 通信半径 `communication_radius`（チェビシェフ距離ではなくユークリッド距離、Vol.1準拠）
- 同一領域制約: 「両者とも場所外」または「両者とも同じ場所内」のペアのみ通信可
- Phase 1 の意思決定時点の位置関係で配送

### 5.4 ステップ実行順序（Vol.1と同一・変更禁止）

1. Phase 1: 全エージェントがLLMでメッセージ決定（JSON: `message`, `reasoning`）
2. Phase 2: 近傍・同一領域のエージェントへ配送
3. Phase 3: 全エージェントがLLMで行動決定（JSON: `action`(move/stay), `direction`(up/down/left/right), `memory`, `reasoning`）
4. Phase 4: 移動実行（1セル、盤外はクランプ）

### 5.5 プロンプト（凍結対象）

- 英語。構成: 状況説明（あなたはAgent N／座標系とplaces位置の説明）＋自己状態（位置・直近memory）＋場所内なら占有数値＋受信メッセージ＋出力JSON仕様
- **定性評価語（danger, safe, comfortable等）・目的の示唆・最適化タスクの明示を含めない**
- `prompts.py` に全文を定数として置き、diff で変更が見えるようにする
- **初回の人間承認後、意味論の変更は Su の明示承認なしに禁止**（Vol.1 CLAUDE.md §6 の継承）

### 5.6 LLM呼び出し

- Ollama `/api/chat`、`format: "json"` は**使わない**（モデル間のJSONモード実装差自体が観察対象を汚すため。素の出力から最初の `{...}` を抽出してパース）
- パース失敗時: リトライ1回→失敗なら `parse_errors.jsonl` に生出力を記録し、当該フェーズはno-op（message空／stay）として続行。**シミュレーションを落とさない**
- 全ファイルI/Oは `encoding="utf-8"` を明示（Vol.1のcp932起因クラッシュの再発防止）
- タイムアウト120s、接続エラーはリトライ3回（指数バックオフ）後にrun中断＋その旨をログ

### 5.7 ログ（run開始時に `output_<name>/` を作成）

- `messages.jsonl`: `{step, sender_id, sender_bloc, sender_model, receiver_ids, message, reasoning}`
  — **配送が成立したもののみ**記録（Vol.1準拠）。ただし空メッセージ率の診断のため `phase1_raw.jsonl` に全Phase 1出力も記録（Vol.1でmessages.jsonl空の原因切り分けに苦労した教訓の反映）
- `memory_reasoning.jsonl`: `{step, agent_id, bloc, model, position, action, direction, memory, reasoning}`
- `run_meta.json`: config全文スナップショット、seed、開始/終了時刻、各モデルの `ollama show` digest、パース失敗率
- `parse_errors.jsonl`: `{step, agent_id, phase, raw_output}`

### 5.8 語彙伝播測定（tools/vocab_metrics.py）

入力: run ディレクトリ。処理:

1. messages + memory の全テキストを小文字化・記号除去・空白分割（依存を増やさない素朴なトークン化。ストップワードは同梱の小リスト）
2. ブロック別頻度から**固有語彙**を抽出: 対数オッズ比（Dirichlet平滑化 α=0.01）上位k=20
3. 各固有語について、**他ブロックのエージェントが最初に使用したstep**と以降の使用回数を集計
4. 出力: `vocab_report.md`（ブロック別固有語表＋越境イベント時系列）と `vocab_events.csv`

これがMVP段階での「連鎖・創発が観測できる」ことの最小デモになる。

## 6. Config スキーマ（例: configs/smoke_local.yaml）

```yaml
simulation:
  duration: 15
  half_space_size: 25
  seed: 42
  run_name: smoke_local

blocs:
  - name: alpha
    model: "qwen2.5:3b"
    base_url: "http://localhost:11434"
    num_agents: 2
  - name: beta
    model: "gemma3:4b"
    base_url: "http://localhost:11434"
    num_agents: 2
  - name: neutral
    model: "llama3.2:3b"
    base_url: "http://localhost:11434"
    num_agents: 2

agents:
  communication_radius: 8      # 6体の低密度対策で広め（Vol.1の教訓）
  memory_limit: 20
  memory_size: 5
  message_history_limit: 10
  message_context_size: 3

places:
  - {name: left_bar, center_x: -15, center_y: 0, half_size: 5, capacity: 6}
  - {name: right_bar, center_x: 15, center_y: 0, half_size: 5, capacity: 5}

llm_defaults:
  temperature: 0.2
  max_tokens: 1024
  timeout_s: 120
```

`mvp_demo.yaml` は同構成で num_agents 各4、duration 30、communication_radius 5。

## 7. 受入基準（MVP完成の定義）

- [ ] `python main.py --config configs/smoke_local.yaml` がPC1で完走（15 step、中断なし）
- [ ] 3ブロックすべてで `memory_reasoning.jsonl` にエントリがあり、`bloc`/`model` フィールドが正しい
- [ ] `messages.jsonl` に1件以上の配送記録（0件なら `phase1_raw.jsonl` で原因を切り分けたレポートを添付）
- [ ] パース失敗率が `run_meta.json` に記録されている（閾値では落とさない。値の報告が義務）
- [ ] `vocab_metrics.py` が smoke 出力に対して `vocab_report.md` を生成する
- [ ] README（目的・元パラダイムへのクレジット・実行手順・configスキーマ・ログスキーマ）
- [ ] Windows 11 / PowerShell で手順が動く（パス区切り・エンコーディング注意）

## 8. スケジュール

| 日 | 作業 |
|---|---|
| 8/4 | 本設計書のSu承認、§11の決定、リポジトリ作成 |
| 8/5 | maker: engine 実装 → checker 検証 → 修正 |
| 8/6 | maker: vocab_metrics ＋ smoke 完走 → checker 検証 |
| 8/7 | mvp_demo run（PC1）、README整備、public化 |
| 8/8 | 申請書3設問の執筆・提出（8/9はバッファ） |

---

## 9. Maker ブリーフ（Claude Code / Sonnet 系にそのまま貼る）

```markdown
# Maker — mixed-cognition-sim MVP実装

あなたは maker。成果物は別コンテキストの checker が検証する。checker の承認を
「正しい実装」以外の手段で得ようとすることは失敗である。

## Mission
設計書（MVP設計書_混在系シミュレーション_2026-08-04.md、リポジトリ直下に配置済み）
の §4〜§7 に従い、マルチLLMエージェントシミュレーションのMVPを実装する。
受入基準は設計書 §7。

## Context
- 新規リポジトリ。Python 3.10+、Windows 11 / PowerShell 環境で動作すること
- Ollama が http://localhost:11434 で稼働、モデル qwen2.5:3b / gemma3:4b /
  llama3.2:3b は取得済みの前提（無ければ ollama pull を人間に依頼して停止）
- 依存は requests / pyyaml のみ。追加したい場合は理由を添えて停止し人間に確認

## 絶対制約
1. ryukih/llm-agents-simulation および suii00 の既存フォークのコードを
   読まない・検索しない・参照しない。挙動仕様は設計書のみを正とする
2. prompts.py のプロンプト文面は設計書 §5.5 の制約で書き、実装後に一度だけ
   人間承認を受ける。承認後の意味論変更は禁止（バグ修正での文字列変更も報告必須）
3. プロンプトに定性評価語・目的示唆・bloc/モデル情報を入れない
4. 全ファイルI/Oに encoding="utf-8" を明示する
5. ステップ実行順序（Phase 1→2→3→4）と通信制約を変更しない

## Steps
1. 設計書 §4〜§7 を全文読む
2. engine/ を実装（world → agent → llm_client → prompts → config → sim の順）
3. configs/smoke_local.yaml を設計書 §6 の通り作成
4. smoke を実行し、§7 の受入基準を自己チェック
5. tools/vocab_metrics.py を実装し smoke 出力で動作確認

## Stopping condition
受入基準を満たしたら停止して報告。または同一の失敗に3回連続で
詰まったら、正直に状況を報告して停止（スコープ拡大で回避しない）。

## Evidence to report
smoke 実行の標準出力末尾、生成された jsonl の先頭3行ずつ、run_meta.json 全文、
vocab_report.md、変更ファイル一覧。証拠を示せない主張はしない。

## Human review gate
- prompts.py の文面確定（初回承認）
- git push / リポジトリ public 化は人間が行う。あなたは commit まで

## Handoff note（checker宛、最後に必ず書く）
変更点・設計書から外れた判断とその理由・自信のない箇所を2〜6行で。
```

## 10. Checker ブリーフ（Claude Code / Opus 系にそのまま貼る）

```markdown
# Checker — mixed-cognition-sim MVP検証

あなたは checker。この実装は書いていない。maker の説明ではなく
アーティファクトから判断せよ。根拠のない PASS が最悪の結果である。

## Inputs
リポジトリ一式、設計書（§4〜§7が仕様）、maker の handoff note。

## Do, independently
1. smoke を自分で再実行する（貼られた出力を信用しない）:
   python main.py --config configs/smoke_local.yaml
2. 設計書 §7 の受入基準を1項目ずつ、実物のファイルで確認
3. 境界チェック:
   - prompts.py に定性評価語・目的示唆・bloc/モデル名の混入がないか全文読む
   - コード中に元リポジトリ由来と思われる痕跡（変数名の不自然な一致、
     コメント、未使用の互換コード）がないか
   - encoding="utf-8" が全ファイルI/Oにあるか grep で確認
   - Phase 順序・通信制約（同一領域条件）が §5.3〜5.4 通りか sim.py を読んで確認
4. jsonl スキーマを §5.7 と突合（フィールド名・型）
5. vocab_metrics.py を別の run ディレクトリ名でも動かし、パス決め打ちがないか確認
6. maker が自信がないと述べた箇所＋maker が触れていない箇所を1つ選んで検査

## Verdict format
- PASS / FAIL（FAILは各項目に証拠を付す）
- 非ブロッキング指摘（最大5件）
- 実行したコマンドと実際の出力
自分で再実行していない項目を根拠に PASS を出してはならない。
```

---

## 11. Su が決める未確定事項

1. **リポジトリ名／プロジェクト名**（仮: mixed-cognition-sim）
2. **ライセンス**: コード非流用のためGPL継承義務はない見立てだが、(a) 敬意としてGPL v3継承 (b) MIT/Apache-2.0＋READMEクレジット のどちらか
3. **smoke の3系統**: 提案は qwen2.5:3b / gemma3:4b / llama3.2:3b（PC1の12GBに同時常駐可能な見積り。qwen2.5:7b を alpha に使う場合は合計約10GBで動くはずだが要実測）
4. **`OLLAMA_MAX_LOADED_MODELS=3` / `keep_alive` の設定**（モデルスワップのスラッシング防止。インストール版のOllamaで要確認）
